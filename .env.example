# Environment Configuration
# Copy this file to .env and update with your actual values

# Flask Configuration
SECRET_KEY=your-super-secret-key-change-in-production
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production
FLASK_ENV=development

# Database Configuration - USE SQLITE FOR NOW
DATABASE_URL=sqlite:///results.db

# Comment out or remove PostgreSQL lines
# DATABASE_URL=postgresql://result_admin:your_db_password@localhost:5432/results_db

# Database Configuration
# For SQLite (Development):
# DATABASE_URL=sqlite:///results.db

# For PostgreSQL (Production):
DATABASE_URL=postgresql://result_admin:your_db_password@localhost:5432/results_db
DB_PASSWORD=your_db_password

# Cloud Deployment (Azure Example)
AZURE_SQL_SERVER=your-server.database.windows.net
AZURE_SQL_DATABASE=results_db
AZURE_SQL_USER=sqladmin
AZURE_SQL_PASSWORD=your_sql_password

# AWS RDS (Alternative)
# DATABASE_URL=postgresql://admin:password@your-rds-endpoint.amazonaws.com:5432/results_db

# Security
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# Optional: Intel SGX Configuration (if using hardware TEE)
SGX_ENABLED=false
SGX_ENCLAVE_PATH=/path/to/enclave
```

**Copy everything from the first `#` comment to the last line `SGX_ENCLAVE_PATH=/path/to/enclave`**

---

## 🎯 Quick Reference: What to Copy for EACH File

Here's a summary of ALL files and what to copy:

| File Name | What to Copy | Where to Find It |
|-----------|--------------|------------------|
| **app.py** | The entire Python code starting with `# app.py - Main Flask Application` | Artifact titled "Flask Backend - app.py" |
| **requirements.txt** | The list of packages (Flask==3.0.0, etc.) | Artifact titled "requirements.txt" |
| **Dockerfile** | Everything from `# Dockerfile for Flask Backend` to the last CMD line | Artifact titled "Dockerfile" |
| **docker-compose.yml** | Everything from `version: '3.8'` to `driver: local` | Artifact titled "docker-compose.yml" |
| **.env.example** | All the environment variable lines with comments | Artifact titled ".env.example" |
| **DEPLOYMENT_GUIDE.md** | The entire markdown document | Artifact titled "DEPLOYMENT_GUIDE.md" |
| **README.md** | The entire markdown document | Artifact titled "README.md - Project Documentation" |
| **test_api.py** | The entire Python test code | Artifact titled "test_api.py - Automated Tests" |
| **setup.sh** | The entire bash script | Artifact titled "setup.sh - Quick Setup Script" |

---

## 💡 Easy Way to Know What to Copy

For each artifact I created above, you'll see a **code block** (gray box with text). 

**Select ALL the text inside that gray box and copy it!**

The artifacts appear like this:
```
┌─────────────────────────────────┐
│ Artifact Title Here             │
├─────────────────────────────────┤
│ [Copy/Paste button]             │
│                                 │
│ CODE STARTS HERE               │
│ ... (all the code)             │
│ CODE ENDS HERE                 │
└─────────────────────────────────┘
```

Just click inside the gray area, press **Ctrl + A** (select all), then **Ctrl + C** (copy), then paste into your file!

---

## ✅ Verification Checklist

After creating all files, your folder should contain:
```
✅ app.py                  (Python code, ~400+ lines)
✅ requirements.txt        (9 lines of package names)
✅ Dockerfile             (No extension, ~35 lines)
✅ docker-compose.yml     (~60 lines, starts with "version: '3.8'")
✅ .env.example           (~30 lines, all comments and KEY=value pairs)
✅ DEPLOYMENT_GUIDE.md    (Very long markdown document)
✅ README.md              (Very long markdown document)
✅ test_api.py            (Python test code, ~300+ lines)
✅ setup.sh               (Bash script, ~100+ lines)